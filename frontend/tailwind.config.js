/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ['./src/**/*.{js,jsx,ts,tsx}'], // Đường dẫn để Tailwind quét file
  theme: {
    extend: {}, // Thêm tuỳ chỉnh (nếu cần)
  },
  plugins: [],
};
